# Hinweis: Compiled-Pakete (psycopg2, pandas, numpy, scikit-learn) als
# Mindestversionen statt fester Pins, damit pip zur jeweiligen Python-Version
# passende Wheels wählt (feste Pins scheiterten unter Python 3.13/3.14 am
# Source-Build, z. B. psycopg2 ohne pg_config, numpy ohne Compiler).

# Datenbank
psycopg2-binary>=2.9.10
SQLAlchemy>=2.0.30

# Daten
pandas>=2.2.2
requests>=2.32.3
holidays>=0.46

# Prognosemodelle (models(Nico)/)
# futureexpert pinnt numpy<2, wofür es ab Python 3.13 keine Wheels mehr gibt.
# Der Pin ist rein konservativ – futureexpert läuft nachweislich mit numpy 2.
# Ab Python 3.13 deshalb manuell installieren (Marker unten überspringt es):
#   pip install futureexpert --no-deps
futureexpert>=3.1.3; python_version < "3.13"
pydantic>=2.5
authlib>=1.3
tenacity>=8.2
httpx>=0.27
cachetools
scikit-learn>=1.5
numpy>=1.26.4

# Qualität / Linting
black>=24.4.2

# Konfiguration
python-dotenv>=1.0.1
